hooked-plugin: "vanilla"
hooked-id: "book"
roll-strategy: "CUSTOM"

custom-strategy:
  first-slot: # 附魔台第一个附魔槽设置
    enchantment-pool:  # 附魔池
      - id: "minecraft:sharpness" # 魔咒 ID
        weight: 50 # 权重
        level: { int-provider: uniform, min: 1, max: 4 } # 等级
        failure: { int-provider: uniform, min: 10, max: 70 }
      - id: "minecraft:looting"
        weight: 50
        level: { int-provider: uniform, min: 1, max: 4 }
        failure: { int-provider: uniform, min: 10, max: 70 }

  second-slot: # 附魔台第二个附魔槽设置
    enchantment-pool: # 附魔池
      - id: "minecraft:sharpness"
        weight: 5
        level: { int-provider: uniform, min: 1, max: 4 }
        failure: { int-provider: uniform, min: 10, max: 70 }
      - id: "minecraft:looting"
        weight: 2
        level: { int-provider: uniform, min: 1, max: 4 }
        failure: { int-provider: uniform, min: 10, max: 70 }
    preconditions:
      - type: permission
        permission: "enchantingtable.use"
      - type: ">="
        value1: "%player_level%"
        value2: 20
      - type: "has_item"
        hooked-plugin: "vanilla"
        hooked-id: lapis_lazuli
        count: 3
    actions: # 可选, 覆写默认的附魔行为
      - type: send_message
        message: "你使用了二号附魔栏"
      - type: cost_level
        cost: 5
      - type: take_item
        hooked-plugin: "vanilla"
        hooked-id: lapis_lazuli
        count: 1
    active-slot-item: # 可选, 覆写默认的UI物品及描述, 当此栏附魔可进行时
      lore:
        - "{original_lore}" # 带有 {original_lore} 的行, 整一行会被替换成结果附魔书的lore;
        - " "
        - "<!i><white><image:enchantment:exp_point> 5 等级"
        - "<!i><white><image:enchantment:lapis_lazuli> 1 青金石"
        - " "
        - "<!i><#a5acb8>→ 点击进行附魔"
      custom-model-data: 31001
      item-model: "minecraft:paper"
    inactive-slot-item: # 可选, 覆写默认的UI物品及描述, 当此栏附魔不可进行时
      item-name: "<red>材料不足"
      lore:
        - "<!i><gray>解锁这个槽位您需要:"
        - " "
        - "<!i><white><image:enchantment:exp_point> 5 等级"
        - "<!i><white><image:enchantment:lapis_lazuli> 1 青金石"
        - " "
        - "<!i><red>→ 无法进行附魔"
      custom-model-data: 31001
      item-model: "minecraft:paper"


  third-slot:
    enchantment-pool:
      - id: "minecraft:sharpness"
        weight: 50
        level: { int-provider: uniform, min: 1, max: 4 }
        failure: { int-provider: uniform, min: 10, max: 70 }
      - id: "minecraft:looting"
        weight: 50
        level: { int-provider: uniform, min: 1, max: 4 }
        failure: { int-provider: uniform, min: 10, max: 70 }
